import { pubsub } from "./pubsub";

class ToDoAdder {
  constructor(currentToDoProject, taskInputField, taskListField) {
    this.currentToDoProject = currentToDoProject;
    this.taskInputField = taskInputField;
    this.taskListField = taskListField;
  }

  renderTaskList() {}



}

class ToDoDOMManager {
    constructor() {
        
    }
}

// pubsub.subscribe("ToDoCreated", renderTaskList())
